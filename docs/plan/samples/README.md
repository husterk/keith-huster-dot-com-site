# Samples

Starter files referenced by the plan. They are drafts written against Astro 7.3 / @astrojs/cloudflare 14 / Wrangler 4.134 (versions verified 2026-09-18, see ../README.md) and have **not** been run; expect to adjust the adapter's output paths in `wrangler.jsonc`, the 1Password item names in the workflows, and the Turnstile/Resend keys. Copy them into the new repository at the paths implied by their names (`github/` → `.github/`, `renovate.json` at the root, `src/pages/api/`, `src/content/`, `tests/`, root).
